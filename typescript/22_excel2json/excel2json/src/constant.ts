export enum EnumOutType {
    CLIENT = 'c',
    SERVER = 's',
}

export enum EnumDataType {
    ANY = 'any',
    BOOL = 'bool',
    INT = 'int',
    NUMBER = 'number',
    STRING = 'string',

    ARRAY_ANY = 'array:any',
    ARRAY_BOOL = 'array:bool',
    ARRAY_INT = 'array:int',
    ARRAT_STRUBG = 'array:string',
}
