export enum EnumOutType {
    CLIENT = 'c',
    SERVER = 's',
}

export enum EnumDataType {
    ANY = 'any',
    BOOL = 'bool',
    INT = 'int',
    NUMBER = 'number',
    STRING = 'string',

}
