<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Openbanner3"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".banner.ui.GalleryActivity" />
        <activity android:name=".banner.ui.TouTiaoActivity" />
        <activity android:name=".banner.ui.TVActivity" />
        <activity android:name=".banner.ui.RecyclerViewBannerActivity" />
        <activity android:name=".banner.ui.ConstraintLayoutBannerActivity" />
        <activity android:name=".banner.ui.VideoActivity" />
        <activity
            android:name=".banner.ui.Vp2FragmentRecyclerviewActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>