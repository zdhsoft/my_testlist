<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/vp_content"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"/>

    <RadioGroup
        android:id="@+id/rg_tabbar"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/rb_home"
            style="@style/TabButton"
            android:checked="true"
            android:text="@string/navi_home"
            android:drawableTop="@drawable/tab_first_selector" />

        <RadioButton
            android:id="@+id/rb_class"
            style="@style/TabButton"
            android:text="@string/navi_group"
            android:drawableTop="@drawable/tab_second_selector" />

        <RadioButton
            android:id="@+id/rb_cart"
            style="@style/TabButton"
            android:text="@string/navi_car"
            android:drawableTop="@drawable/tab_third_selector" />
        <!--
        <RadioButton
            android:id="@+id/rb_cart1"
            style="@style/TabButton"
            android:text="@string/navi_car1"
            android:drawableTop="@drawable/tab_third_selector" />
        <RadioButton
            android:id="@+id/rb_cart2"
            style="@style/TabButton"
            android:text="@string/navi_car2"
            android:drawableTop="@drawable/tab_third_selector" />
            -->
    </RadioGroup>

</LinearLayout>